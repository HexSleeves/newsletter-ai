{
  "private": true,
  "name": "with-vite",
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "turbo": "^2.6.1"
  }
}
